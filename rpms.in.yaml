contentOrigin:
  repofiles:
    - ./rpms.repo
    - ./compose.repo
context:
  bare: true

arches:
  - x86_64
  - ppc64le
  - s390x

# taken from install package list in Dockerfile
packages:
  - haproxy22
